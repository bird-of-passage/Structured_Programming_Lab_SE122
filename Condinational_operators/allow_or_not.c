#include <stdio.h>

int main()
{
    float gpa = 4.00;
    int age = 18;

    if (gpa == 4.00 || age == 18)
    {
        printf("You are allowed");
    }
    else
    {
        printf("You are not allowed");
    }

    return 0;
}